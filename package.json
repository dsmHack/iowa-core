{
  "name": "iowa_core",
  "version": "1.0.0",
  "scripts": {
    "build": "next build",
    "dev": "next",
    "deploy": "NODE_ENV=gh-pages yarn build && touch out/.nojekyll && gh-pages -t -d out",
    "export": "next export",
    "serve": "cd out && serve -p 3000",
    "start": "next start"
  },
  "license": "ISC",
  "dependencies": {
    "isomorphic-unfetch": "^3.0.0",
    "next": "latest",
    "react": "^16.12.0",
    "react-dom": "^16.12.0"
  },
  "devDependencies": {
    "gh-pages": "^2.2.0",
    "serve": "^11.3.0"
  }
}
